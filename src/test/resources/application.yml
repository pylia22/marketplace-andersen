spring:
  jpa:
    hibernate:
      ddl-auto: update
  datasource:
    hikari:
      maximum-pool-size: 10
      max-lifetime: 60000
      connection-timeout: 30000
      idle-timeout: 30000
      minimum-idle: 5